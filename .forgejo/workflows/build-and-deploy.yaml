name: Build and Deploy to Neocities

on:
  push:
  workflow_dispatch:

env:
  NEOCITIES_API_KEY: ${{ secrets.NEOCITIES_API_KEY }}

jobs:
  build:
    runs-on: native
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Clone template and merge repos
        run: |
          # Setup directories
          TEMPLATE_REPO="https://git.chobble.com/chobble/chobble-template.git"
          TEMPLATE_DIR="${{ github.workspace }}/template"
          BUILD_DIR="${{ github.workspace }}/build"
          DEPLOY_PATH="${{ github.workspace }}/dist"
          
          # Clone the template repo
          git clone "$TEMPLATE_REPO" "$TEMPLATE_DIR"
          
          # Create build directory
          mkdir -p "$BUILD_DIR"
          mkdir -p "$DEPLOY_PATH"
          
          # Copy template files first
          rsync \
            --recursive \
            --update \
            --delete \
            --exclude=".git" \
            --exclude="node_modules" \
            --exclude="*.md" \
            "$TEMPLATE_DIR/" "$BUILD_DIR/"
          
          # Overlay our repo files on top
          rsync \
            --recursive \
            --update \
            --exclude=".git" \
            --exclude="*.nix" \
            --exclude="README.md" \
            --exclude=".forgejo" \
            "${{ github.workspace }}/" "$BUILD_DIR/src/"
          
          # Save paths for next steps
          echo "BUILD_DIR=$BUILD_DIR" >> $GITHUB_ENV
          echo "DEPLOY_PATH=$DEPLOY_PATH" >> $GITHUB_ENV

      - name: Update site.json with secrets
        run: |
          sed -i 's/"formspark_id": "[^"]*"/"formspark_id": "${{ secrets.FORMSPARK_ID }}"/g' "$BUILD_DIR/src/_data/site.json"
          sed -i 's/"botpoison_public_key": "[^"]*"/"botpoison_public_key": "${{ secrets.BOTPOISON_PUBLIC_KEY }}"/g' "$BUILD_DIR/src/_data/site.json"

      - name: Build site
        run: |
          cd $BUILD_DIR
          
          # Install dependencies
          yarn install
          
          # Build the site
          yarn eleventy
          
          # Copy built site to deploy directory
          cp -r _site/* $DEPLOY_PATH/

      - name: Deploy to Bunny
        uses: R-J-dev/bunny-deploy@v2.0.6
        with:
          access-key: ${{ secrets.BUNNY_ACCESS_KEY }}
          directory-to-upload: "./dist"
          storage-endpoint: "https://uk.storage.bunnycdn.com"
          storage-zone-name: "southport-organics"
          storage-zone-password: ${{ secrets.BUNNY_STORAGE_ZONE_PASSWORD }}
          concurrency: "5"
          enable-delete-action: true
          enable-purge-pull-zone: true
          pull-zone-id: "3576287"
          replication-timeout: "15000"
